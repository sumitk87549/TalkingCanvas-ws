<div class="home-container">
  <!-- Hero Section -->
  <section class="hero" *ngIf="featuredPaintings.length > 0">
    <div class="hero-content">
      <div class="hero-text">
        <h1 class="animate-fade-in">Where Art Speaks Volumes</h1>
        <p class="animate-fade-in animate-delay-1">Discover and collect unique oil paintings from the world's most talented artists</p>
        <div class="hero-buttons animate-fade-in animate-delay-2">
          <a routerLink="/paintings" class="btn btn-primary">Explore Collection</a>
          <a routerLink="/artists" class="btn btn-outline">Meet Our Artists</a>
        </div>
      </div>
      <div class="hero-image">
        <div class="floating-image">
          <img [src]="featuredPaintings[0]?.image || 'assets/images/placeholder-painting.jpg'" alt="Featured Painting" class="floating">
        </div>
      </div>
    </div>
    <div class="scroll-indicator">
      <span>Scroll to explore</span>
      <div class="arrow-down"></div>
    </div>
  </section>

  <!-- Loading State
  <div *ngIf="featuredPaintings.length === 0" class="loading-container">
    <div class="spinner"></div>
    <p>Loading amazing artworks...</p>
  </div> -->

  <!-- Featured Paintings -->
  <section class="featured-paintings" *ngIf="featuredPaintings.length > 0">
    <div class="section-header">
      <h2>Featured Paintings</h2>
      <p>Handpicked selection of our finest artworks</p>
    </div>
    <div class="painting-grid">
      <div class="painting-card" *ngFor="let painting of featuredPaintings">
        <div class="painting-image">
          <img [src]="painting.image" [alt]="painting.title" (error)="onImageError($event)">
          <div class="painting-overlay">
            <h3>{{painting.title}}</h3>
            <p>{{painting.artist}}</p>
            <span class="price">{{painting.price}}</span>
          </div>
        </div>
      </div>
    </div>
    <div class="section-footer">
      <a routerLink="/paintings" class="btn btn-secondary">View All Paintings</a>
    </div>
  </section>

  <!-- Artist Showcase -->
  <section class="artist-showcase" *ngIf="featuredArtists.length > 0">
    <div class="section-header">
      <h2>Meet Our Talented Artists</h2>
      <p>Discover the creators behind the masterpieces</p>
    </div>
    <div class="artist-grid">
      <div class="artist-card" *ngFor="let artist of featuredArtists">
        <div class="artist-image">
          <img [src]="artist.image" [alt]="artist.name" (error)="onImageError($event)">
        </div>
        <div class="artist-info">
          <h3>{{artist.name}}</h3>
          <p class="specialty">{{artist.specialty}}</p>
          <p class="bio">{{artist.bio}}</p>
          <a [routerLink]="['/artists', artist.id]" class="btn btn-text">View Profile →</a>
        </div>
      </div>
    </div>
  </section>

  <!-- Testimonials -->
  <section class="testimonials" *ngIf="testimonials.length > 0">
    <div class="section-header">
      <h2>What Collectors Say</h2>
      <p>Experiences from our community</p>
    </div>
    <div class="testimonial-slider">
      <div class="testimonial-card" *ngFor="let testimonial of testimonials">
        <div class="quote-icon">"</div>
        <p class="testimonial-text">{{testimonial.text}}</p>
        <div class="testimonial-author">
          <img [src]="testimonial.avatar" [alt]="testimonial.name" (error)="onImageError($event)">
          <div>
            <h4>{{testimonial.name}}</h4>
            <p class="role">{{testimonial.role}}</p>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- Newsletter -->
  <section class="newsletter" *ngIf="featuredPaintings.length > 0">
    <div class="newsletter-content">
      <div class="newsletter-text">
        <h2>Stay Updated</h2>
        <p>Subscribe to our newsletter for new arrivals, exclusive offers, and artist spotlights</p>
      </div>
      <form class="newsletter-form" (ngSubmit)="onSubscribe($event)">
        <input type="email" placeholder="Your email address" required>
        <button type="submit" class="btn btn-primary">Subscribe</button>
      </form>
    </div>
  </section>
</div>
