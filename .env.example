# TalkingCanvas Environment Configuration Template
# ================================================
# Copy this file to .env and fill in your values:
#   cp .env.example .env
#
# IMPORTANT: Never commit .env to Git!

# ===========================================
# DATABASE
# ===========================================
DB_PASSWORD=YourSecureDbPassword2024

# ===========================================
# JWT SECRET (REQUIRED)
# ===========================================
# Use a strong, random 64+ character string
# Generate one: openssl rand -base64 64
JWT_SECRET=ReplaceWithYourSecureRandomKey64CharactersMinimumForProductionSecurity

# ===========================================
# MAIL (Optional - for password reset emails)
# ===========================================
# Create an App Password: Google Account > Security > 2FA > App Passwords
MAIL_USERNAME=your-email@gmail.com
MAIL_PASSWORD=your-app-password

# ===========================================
# CORS ALLOWED ORIGINS
# ===========================================
# Add your laptop's IP for local network access
# Find IP: ipconfig (Windows) or ip addr show (Linux)
CORS_ORIGINS=http://localhost:4200,http://localhost:8088,http://localhost,http://YOUR_IP:4200,http://YOUR_IP:8088
